{
  "name": "hungry-ghost-dzn",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "api"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c auto \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w client",
    "build:all": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server",
    "start:prod": "NODE_ENV=production npm run start -w server",
    "preview": "npm run preview -w client",
    "install:all": "npm install && npm install -w client && npm install -w server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "nodemailer": "^8.0.1"
  }
}
